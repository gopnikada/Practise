using System;
using System.Collections;
using System.Collections.Generic;
using System.ComponentModel;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace Practise
{
    class Lesson4
    {

        /*ArrayList - for dynamic arrays, containing only Objects
         *  ArrayList Commands
         *  
         * Add(object) - add new one to the end
         * Clear() - clear all the array
         * Count() - like Length
         * Remove(object) - remove some object
         * RemoveAt(int) - remove some object linging to index
         * ToArray() - makeing ArrayList the object Array
         * ToArray(Type) - makeing ArrayList the some type Array(getting the type - typeof(string))
         * 
         * 
         * 
         * List<T> - 
         * List<string> = new List<string>();
         * access via indexing
         * 
         * 
         * Dictionary(key - value)
         * 
         * Dictionary<T1(key),T2(value)>
         * 
         * Dictionary<string, double> dict = new Dictionary<string, double>();
         * Add(T1,T2) - add element, error when existing key occurs
         * Clear()
         * ContainsKey(T1),ContainsValue(T2) - check if element exists(returns true/false)
         * Count()
         * Keys, Values - respectivle collections 
         * Remove(T1) - remove according to the key
         * 
         * 
         * foreach(readonly) - for all the collectionis + which realise IEnumerable
         *
         *List<string> lst = new List< string>();
         *foreach (string element in lst)
         *{
         *  string str = element;
         *}
         *Work with Files - ReadFile(), SaveToFile()
         */
    }
}
